# 更新记录

## 1.3.0

通信版本升到 6，照常连接通信版本 5 的宿主，旧接入库 1.2.0 也照常能连通信版本 6 的宿主。

- 消息卡片可带回复输入条（`reply`），用户发送的文字经 `IslandClient.onReply` 交回；音乐卡片进度条可拖（`seekable`），松手的位置经 `IslandClient.onSeek` 交回。锁屏时不显示输入条。
- 通用卡片标题右边放大的数值（`value`，可带颜色和小图）、说明下面的横图（`strip`）、进度条上跟着进度走的小图（`marker`）、按系统符号画的单色小图（`symbol`）与符号角标（`heroBadge`）。
- 胶囊的转圈亮弧（槽位 `busy`）、图标明暗变化（`breathing`）、横屏短字（`landscapeText`）；消息发送时间（`postedAtWallMs`）；锁屏时单独藏起的按钮（`requiresUnlock`）。
- 跟自己的通知去重：`notificationId` / `notificationTag` 说明这条内容对应本应用的哪条通知，那条通知不再单独上岛，对应内容显示时也不弹系统横幅。
- 排位：外部内容的排位上限从 80 改为 60（与还没到点的计时同一档），不再压过通话中、刚到的新消息和导航；`MESSAGE` 种类按新消息处理，刚到先占主岛，过「消息主岛停留时长」后让位。
- 说明补全：计时、状态、音乐卡片认的按钮编号，各模板的排法，横屏显示，哪些内容不用接入库。

## 1.2.0

接入库只发现星流内置宿主（带 `com.astraisland.HOST_PROTOCOL` 元信息的正式／调试包）：独立星河岛应用已停止发布，本版起不再作为宿主，仅安装旧版独立岛的设备请改装星流。通信版本保持 5，普通投送用法不变。

## 2026-09-21

独立星河岛应用停止发布并从发布附件移除：星河岛能力由星流安装包完整提供，接入不再需要独立应用。接入库与通信版本不变，仍兼容已安装旧版独立星河岛的设备。

这是当天尚未改接入库时的情况。上面 1.2.0 起不再把已安装的旧独立包当作宿主。不要把本段「仍兼容」读成现行规则，也不要改写成当天就已经拒绝旧包。

## 1.1.0

接入库新增发现星流内置宿主：同时支持独立星河岛与带宿主标识的星流正式／调试安装包，通信版本保持 5，普通投送用法不变。旧版 1.0.0 只发现独立星河岛。

## 1.0.0

提供接入库、独立示例、字段清单和开发文档，支持普通模板与自绘卡片。
